services:
  mongo:
    image: mongo:8
    container_name: pagerduty_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
